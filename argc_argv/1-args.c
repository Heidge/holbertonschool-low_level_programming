/**
 * main - function
 *
 * @argc: number of arg
 * @argv: valor of arg
 * Return: always 0
 */
#include <stdio.h>
int main(int argc, char **argv)
{
	int i = 0;

		argv[i]++;
		printf("%d\n", argc - 1);

	return (0);
}

#include "main.h"
/**
 *
 *
 */
void times_table(void)
{
	int x, y;

	for (x = 0; x <= 9; x++)
	{
		for (y = 0; y <= 9; y++)
		{
		_putchar(x * y);
	        _putchar(',');
		_putchar(' ');
		}
		_putchar('\n');
	}
}

#include <stdio.h>

/**
* main - Printf last digit of a random number and say if >5, =0 or less than 6
* Return: always 0
*/

int main(void)
{
    int i = 48;

    for (; i <= 57 ; i++)
    {
        putchar(i);
    }
    putchar('\n');
    
    return (0);
}
